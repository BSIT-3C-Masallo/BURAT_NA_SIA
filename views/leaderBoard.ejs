<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Leaderboard Quiz</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/leaderBoard.css">
</head>
<body>
  <div class="leaderboard-container">
    <h1>Leaderboard Quiz: Results</h1>
    <table>
      <thead>
        <tr>
          <th>Position</th>
          <th>Name</th>
          <th>Entered On</th>
          <th>Points</th>
          <th>Result</th>
        </tr>
      </thead>
      <tbody id="leaderboard">
        <!-- Loop through the leaderboardData array and populate the table rows -->
        <% leaderboardData.forEach((entry, index) => { %>
          <tr>
            <td><%= index + 1 %></td> <!-- Position is index + 1 -->
            <td><%= entry.Username %></td>
            <td><%= entry.Date_Finished.toLocaleString() %></td> <!-- Display formatted date -->
            <td><%= entry.Points %></td>
            <td><%= entry.Results %>%</td> <!-- Results as percentage -->
          </tr>
        <% }) %>
      </tbody>
    </table>
    <div class="button-container">
      <a href="/quizProtoPage" class="btn btn-primary mt-4">Try Again</a>
      <a href="/AboutUs" class="btn btn-primary mt-4">About Us</a>
    </div>
  </div>
</body>
</html>
